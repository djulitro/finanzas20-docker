# ============================================
# CONFIGURACIÓN DEL PROYECTO
# ============================================
# Cambia solo estas variables para adaptar a tu proyecto

# Nombre del proyecto (minúsculas, sin espacios)
PROJECT_NAME=

# Nombre del proyecto (formato display)
PROJECT_DISPLAY_NAME=

# Puertos externos
PORT_BACKEND=8081
PORT_BACKEND_SSL=4431
PORT_FRONTEND=3001
PORT_MYSQL=3307
PORT_REDIS=6380
PORT_PHPMYADMIN=8082

# Configuración de Base de Datos
DB_DATABASE=${PROJECT_NAME}_db
DB_USERNAME=${PROJECT_NAME}_user
DB_PASSWORD=${PROJECT_NAME}_password
DB_ROOT_PASSWORD=root_${PROJECT_NAME}_2024

# URLs
APP_URL=http://localhost:${PORT_BACKEND}
FRONTEND_URL=http://localhost:${PORT_FRONTEND}
API_URL=http://localhost:${PORT_BACKEND}/api
